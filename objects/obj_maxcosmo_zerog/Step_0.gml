move_player(1, obj_inv_floor);

player_death(obj_death_block);

transition(obj_flag, rm_level1_2);

transition(obj_rocket_static, rm_level1_3);

transition(obj_inv_floor_1, rm_level1_3);

transition(obj_trans_level, rm_level2_3);

fix_block(obj_spark);